% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trainWeightedClock.R
\name{trainWeightedClock}
\alias{trainWeightedClock}
\title{Train a Cell Type Specific Clock}
\usage{
trainWeightedClock(
  dataMatrix,
  responseVector,
  candidateFeatures,
  featureWeights = NULL,
  alpha = 0.5,
  nfolds = 10,
  lambda.min.ratio = NULL,
  lambda_choice = "lambda.min",
  seed = 42
)
}
\arguments{
\item{dataMatrix}{A numeric matrix of DNAm data (features in rows,
samples in columns).}

\item{responseVector}{A numeric vector representing the target phenotype
(e.g., chronological age). Length must match \code{ncol(dataMatrix)}.}

\item{candidateFeatures}{A character vector of features to include in the
model training. Must intersect with \code{rownames(dataMatrix)}.}

\item{featureWeights}{A named numeric vector of weights applied to the
\code{candidateFeatures}. Note: These are passed as \code{penalty.factor} to
glmnet, meaning a LOWER value indicates a MORE important feature
(less shrinkage). If NULL, defaults to unweighted (all 1s).}

\item{alpha}{The elastic net mixing parameter. Defaults to 0.5.}

\item{nfolds}{Number of folds for cross-validation. Defaults to 10.}

\item{lambda.min.ratio}{Smallest value for lambda, as a fraction of lambda.max.
Useful for extending the lambda sequence if the minimum is hitting the edge. Defaults to NULL.}

\item{lambda_choice}{Character string specifying which lambda value to use
for extracting coefficients. Typically "lambda.min" or "lambda.1se". Defaults to "lambda.min".}

\item{seed}{Integer for random seed to ensure reproducible CV folds.
Defaults to 42.}
}
\value{
A list containing \code{cvModel}, \code{bestLambda}, and \code{clockCoefficients}.
The \code{clockCoefficients} component is a data frame of non-zero coefficients,
including the intercept when present.
}
\description{
Trains an Elastic Net model using pre-selected features and
custom feature weights (penalties). This function serves as the core
predictive model builder for generating custom or cell-type-specific clocks.
}
\examples{
set.seed(1)
data_mat <- matrix(
  rnorm(30 * 20),
  nrow = 30,
  dimnames = list(paste0("cg", 1:30), paste0("Sample", 1:20))
)
response <- seq(30, 68, length.out = 20) + rnorm(20, sd = 1)
candidate_features <- rownames(data_mat)[1:12]
feature_weights <- setNames(seq(0.5, 1.5, length.out = 12), candidate_features)

clock_res <- trainWeightedClock(
  dataMatrix = data_mat,
  responseVector = response,
  candidateFeatures = candidate_features,
  featureWeights = feature_weights,
  alpha = 0.5,
  nfolds = 5,
  seed = 1
)

head(clock_res$clockCoefficients)
}
