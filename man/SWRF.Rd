% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SWRF.R
\name{SWRF}
\alias{SWRF}
\title{Select Cell-Type Specific Features via SWRF}
\usage{
SWRF(
  targetCellType,
  candidateFeatures,
  dataMatrix,
  cellFractions,
  responseVector,
  seed = 42,
  windowSize = 100,
  stepSize = 10,
  nTrees = 1000,
  numThreads = 20
)
}
\arguments{
\item{targetCellType}{A string specifying the target cell type column name
in \code{cellFractions}.}

\item{candidateFeatures}{A character vector of pre-selected feature names
(e.g., DEGs, DMCs) to evaluate.}

\item{dataMatrix}{A numeric matrix of omics data (features in rows,
samples in columns).}

\item{cellFractions}{A data frame of cell type fractions (samples in rows,
cell types in columns).}

\item{responseVector}{A vector representing the target phenotype. Can be
numeric (for regression) or factor/character (for classification).}

\item{seed}{Integer for random seed. Defaults to 42.}

\item{windowSize}{Number of samples per sliding window. Defaults to 100.}

\item{stepSize}{Number of samples to shift the window. Defaults to 10.}

\item{nTrees}{Number of trees for the Random Forest model. Defaults to 1000.}

\item{numThreads}{Number of CPU threads for ranger. Defaults to 20.}
}
\value{
A data frame containing Spearman correlation results for each
feature.
}
\description{
Identifies features associated with a specific phenotype
(continuous or categorical) in a cell-type-specific manner using a
Sliding Window Random Forest (SWRF) approach.
}
\examples{
set.seed(1)
data_mat <- matrix(
  rnorm(20 * 24),
  nrow = 20,
  dimnames = list(paste0("gene", 1:20), paste0("Sample", 1:24))
)
cell_fracs <- data.frame(
  Neuron = seq(0.1, 0.9, length.out = 24),
  Astro = seq(0.9, 0.1, length.out = 24)
)
response <- 50 + 3 * cell_fracs$Neuron + rnorm(24, sd = 0.2)

swrf_res <- SWRF(
  targetCellType = "Neuron",
  candidateFeatures = rownames(data_mat)[1:10],
  dataMatrix = data_mat,
  cellFractions = cell_fracs,
  responseVector = response,
  seed = 1,
  windowSize = 8,
  stepSize = 4,
  nTrees = 50,
  numThreads = 1
)

head(swrf_res)
}
